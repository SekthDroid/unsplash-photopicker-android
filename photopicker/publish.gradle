apply plugin: 'maven-publish'

publishing {
    publications {
        release(MavenPublication) {
            groupId = 'com.github.Sekthdroid'
            artifactId = 'unsplash-photo-picker'
            version = '2.1.0'

            afterEvaluate {
                from components.release
            }
        }
    }
}
